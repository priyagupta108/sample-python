name: Test Non-Existent Path

on: [push]

jobs:
  build:
    runs-on: self-hosted

    steps:
  
    - name: Verify PATH
      run: echo $PATH
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Verify PATH
      run: echo $PATH
