name: Unzip issue reproduce 
on:
  push:
    branches:
      - unzip-issue

jobs:
  clear-cache:
    runs-on: self-hosted
    steps:
    - name: Clear tool cache
      run: |
        rm -rf /path/to/runner/_work/_tool
