name: Reproduce Issue #851

on:
  push:
    branches: [ "issue-851" ]
 
jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Reorder PATH
        run: echo "PATH=/your/path/to/virtualenv/bin:$PATH" >> $GITHUB_PATH
      - uses: actions/setup-python@v4
        with:
           python-version: '3.x'



     

      - run: echo "$PATH"
