name: Clear Python Cache

on:
  push:
    branches:
      - unzip-issue  # Adjust this to the branch you want to trigger the workflow

jobs:
  clear-cache:
    runs-on: self-hosted

    steps:
    - name: Clear setup-python cache
      run: |
        echo "Clearing setup-python cache..."
        rm -rf ~/.setup-python/
        echo "Cache cleared successfully."
